{
  "name": "my-textbook-search-app",
  "version": "1.0.0",
  "main": "server.js",   // Changed "main" to "server.js" (if your file is server.js)
  "scripts": {
    "start": "node server.js",  // Added the "start" script
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "node-fetch": "^2.7.0"
  }
}
